name: Build and Release

on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller 
          pip install -r requirements.txt

      - name: Build EXE
        run: |
          pyinstaller --onefile app.py

      - name: Upload Windows Artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-exe
          path: dist/app.exe

#  build-macos:
#    runs-on: macos-latest
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#
#      - name: Set up Python
#        uses: actions/setup-python@v4
#        with:
#          python-version: '3.10'
#
#      - name: Install dependencies
#        run: |
#          python -m pip install --upgrade pip
#          pip install pyinstaller
#          brew update
#          brew install create-dmg
#
#      - name: Build DMG
#        run: |
#          pyinstaller --name MyApp --windowed --onefile app.py
#          create-dmg --volname "MyApp" --window-size 640 480 --app-drop-link 512 352 dist/MyApp.dmg dist/MyApp.app
#
#      - name: Upload macOS Artifact
#        uses: actions/upload-artifact@v4
#        with:
#          name: macos-dmg
#          path: dist/MyApp.dmg

  release:
#    needs: [build-windows, build-macos]
    needs: [build-windows]
    runs-on: ubuntu-latest
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v3
        with:
          path: artifacts

      - name: Upload to Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/windows-exe/app.exe
            artifacts/macos-dmg/MyApp.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
